#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

entry_points = \
{'console_scripts': ['env-replace = env_replace:main']}

setup(name='env_replace',
      version='1.0.0',
      description='env-replace: un-expand environment variables for more compact output',
      author='Min RK',
      author_email='benjaminrk@gmail.com',
      url='https://github.com/minrk/env_replace',
      py_modules=['env_replace'],
      entry_points=entry_points,
      python_requires='>=3',
     )
