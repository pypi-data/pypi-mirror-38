# xrandom

Random something.

## Installation

```
$ pip3 install xrandom
```

## Usage

- `xrandom.salt()`
- `first, last = xrandom.fake_name()`
- `salt = random.salt()`
- `salt, hint = random.salt(hint=True)`
- ...

