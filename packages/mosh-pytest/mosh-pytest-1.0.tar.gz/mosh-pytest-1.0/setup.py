import setuptools

with open("README.md", "r") as file:
    long_description = file.read()

setuptools.setup(
    name="mosh-pytest",
    version="1.0",
    long_description=long_description,
    packages=[]
)
